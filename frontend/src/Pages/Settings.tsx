import React from 'react';

export default function Settings() {
  return (
    <div style={{ padding: '1rem' }}>
      <h1>Settings</h1>
      {/* TODO: render user‑ or role‑specific settings forms */}
    </div>
  );
}